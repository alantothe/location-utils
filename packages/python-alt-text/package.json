{
  "name": "@url-util/python-alt-text",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn app:app --host 0.0.0.0 --port 8000 --reload",
    "dev:direct": "python3 app.py",
    "install-deps": "python3 -m pip install --user -r requirements.txt",
    "install-deps-system": "pip3 install -r requirements.txt",
    "test": "python3 -c \"print('Python 3 available'); import sys; print(f'Version: {sys.version}')\"",
    "test:python": "bun run test-connection.ts"
  }
}
